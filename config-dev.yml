server:
  port: 12000

spring:
  application:
    name: myshop-config

  cloud:
    config:
      server:
        git:
          uri: https://github.com/haomingz86-ux/springcloud.git
#          default-label: master